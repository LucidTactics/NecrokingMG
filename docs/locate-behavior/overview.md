# Overview — behavior routing map

Entry point for the **locate-behavior** skill. Match the request to an area below, then
open the listed `<area>.md`. Areas marked **(not yet documented)** have no doc yet —
**research them and add the doc** per README.md → "Self-healing", then update this file.

> Hints for undocumented areas are **tentative** (inferred from folder/namespace names,
> not yet verified). Don't trust them as fact — confirm by reading the code, and capture
> what you learn in a new `<area>.md`.

## Documented areas

| Area | Doc | Covers |
|------|-----|--------|
| Game1.* root partials | [game1-partials.md](game1-partials.md) ✅ | Frame loop, input, menu state, orchestration of every system; player spell-cast pipeline, crafting, animation tick, all rendering entry points, map load/save, dev-command dispatch |
| Render/ (pipeline + effects) | [render.md](render.md) ◐ | **The draw-dispatch pipeline** — top-level `GameRenderer.Draw` pass sequence, `EffectBatch` (canonical blend/sampler + flush-with-shader `BeginEffect`/`EndEffectResume*`), `Bloom` RT/compositing, the Y-sort depth list, `Renderer.DrawSprite`, `UIShaders` — plus the visual-effect/flipbook systems (`EffectManager`, `Flipbook`, `ReanimEffectSystem`, particles). Atlas internals, shadows, fonts, HUD widgets still TODO |
| Jobs & workers | [jobs-workers.md](jobs-workers.md) ✅ | Worker economy — Job Board UI (`JobBoardUI`) + Grave Roster UI (`GraveRosterUI`), `WorkerSystem` (grave assignment, stockpiles, the pool→jobs `Dispatch` auto-assigner), `JobDef/JobState/JobRegistry`, `AI/WorkerHandler` FSM |
| Corpses & pickup/carry | [corpses.md](corpses.md) ✅ | `Corpse` data model in `Simulation`, player pickup/carry (`CorpseInteractionManager`, F-key + corpse-hover hit-test in `Game1.cs`), foragable-style proximity gather, where "click corpse → walk → pick up" goes |
| Unit AI archetypes & routines | [ai.md](ai.md) ✅ | `IArchetypeHandler`/`ArchetypeRegistry` (name→id→handler), per-tick dispatch in `Simulation`, archetype assignment at spawn (`Simulation.ApplyDefRuntimeFields` — the single def→unit runtime copy all spawn paths share), `SubroutineSteps` move primitives, `WorkerHandler` FSM template, **where to add a new per-unit behavior**. Legacy `AIBehavior` migration is done (enum = 5 values) |
| World — env objects & foragables | [world.md](world.md) ✅ | `EnvironmentSystem` (env-def + placed-object store, `AddObject`/`FindDef`/`CollectForagable`/respawn), `ForagableSystem` (player pickup arc), death→corpse hook in `RemoveDeadUnits`, and the Vogel-spiral anti-stack packing precedent (`HordeSystem`). Where runtime world-object spawning + foragable-eat goes |
| Dev control server | [dev.md](dev.md) ✅ | The `--devserver` HTTP control channel: transport (`Dev/DevServer.cs` `DevServer`/`DevCommand`, `Dev/DevScript.cs` batch jobs) + every command verb in `Game1.Dev.cs` (`ExecuteDevCommand` switch), `Game1.DevData.cs` (`DevAddData`/state). **Where to add a new dev verb.** |
| Editor/ (UI/widget editor + map editor + shared text-field) | [editor.md](editor.md) ◐ | In-game immediate-mode editors. **Shared editable-text-field/focus mechanism** (`EditorBase.DrawTextField`, single `_activeFieldId`/`_inputBuffer`, `HandleTextInput`) — the source of "typed text bleeds into next selected object". **UI/widget editor** covered in depth — `UIEditorWindow.cs` data models + copy/paste clone logic. **Map editor** — `MapEditorWindow.cs` tab plumbing (`MapEditorTab` enum + TabRow arrays + Update/Draw switches), Objects-tab brush painting (`PaintObjectsBatch`, `CanPlaceObject` spacing, batch undo), ProcGen-tab density brush (`Editor/ProcGenStyle.cs`, `PaintProcGen`/`PlaceProcGenPool`, `data/procgen_styles.json`), `SaveMap` → `assets/maps/<map>.json` `placedObjects`, the shared `overPanel`/`IsMouseOverPanel` world-click guard every tab reuses. Other editors stubbed. |
| Combat — attack resolution & range | [combat.md](combat.md) ✅ | The `PendingAttack` → hit-frame → `ResolvePendingAttack`/`ResolveMeleeAttack` pipeline, the canonical melee-range formula (`Game/Combat/MeleeRangeUtil.cs`, shared by sim + AI), and **why range is gated at stamp-time not resolve-time** — incl. the player click→melee order in `Game1.WorldClicks.cs` `TryAttackClick` |
| UI/ — overlays & panels | [ui.md](ui.md) ✅ | The right-side selected-unit sheet (`UI/UnitInfoPanel.cs`) + where the show-panel decision lives (`Game1.cs` 'U'/'O'/auto-hover, units only), the corpse-pile "inventory" cursor tooltip (`HUDRenderer.DrawObjectTooltip` + `WorkerSystem.PiledCorpseLines`), boar belly store (`Simulation._boarBellies`). Widget renderer + popup stack. |
| Zones — authored map areas (village / wolf-pack / deer-herd / foraging rects) | [zones.md](zones.md) ✅ | `MapZone`/`ZoneKind`/`ZoneSpawnEntry`/`ZoneSystem` (`Game/ZoneTypes.cs`), load-time apply (`Game1.Zones.cs` `ApplyZones`), runtime periodic spawns (`UpdateZoneSpawns` → `TrySpawnZoneUnit`/`TrySpawnZoneForagable`), `MapData.LoadZones`, map-editor Zones tab (draw/select/drag + per-kind left config panel incl. spawn table + `SaveZones` sidecar `data/maps/<map>_zones.json`) |
| Animation — unit anim state & the two-channel resolver | [animation.md](animation.md) ✅ | `AnimState` enum + per-unit `AnimController` (`Render/AnimController.cs`), the `RoutineAnim`/`OverrideAnim` two-channel arbitration (`Render/AnimResolver.cs`), the per-frame tick (`Game1.Animation.cs` `UpdateAnimations`, archetype vs legacy paths), the **full writer census** (AI locomotion, combat flinch/dodge/death, buffs/incap, physics/jump/trample), lock/cooldown mechanisms, why units slide while an override plays |
| Blocking & standability | [blocking.md](blocking.md) ✅ | The "does something block here?" routing page: facade entry points (`_sim.Query.IsSpotBlocked`/`IsWallBlocked`), the single-source primitives (`TileGrid.OverlapsImpassable`, `EnvironmentSystem.GetCollisionCircle`), the three blocking representations (pathfinding tier stamps / runtime collision / placement spacing) and the collision-dirty sync flow |
| Movement — pathfinding, ORCA, collision | [movement.md](movement.md) ✅ | The whole movement stack: sector flow-field pathfinder (`World/Pathfinder.cs` — sector Dijkstra, portal-graph routing via corridor-bounded resumable A*, imaginary-chunk stuck escape, budgeted pathfinding), ORCA solver (`Movement/Orca.cs`), `Simulation.UpdateMovement` (neighbor gather, stuck nudge, accel model, wall collision), horde formation slots (`Game/HordeSystem.cs`), quadtree/env spatial indices, scripted-movement owners (physics/jump/trample) |
| Data/ — JSON registries & serialization | [data-registries.md](data-registries.md) ✅ | The id-keyed `data/*.json` registries: `RegistryBase<TDef>` = the single shared System.Text.Json load/save engine (`CreateJsonOptions`/`SerializeItem`/`Load`/`Save`, atomic if-changed writes), `GameData.Load/Save` = the save-everything aggregate every editor Save button calls, `UnitDef` schema + `weapon_points.json` sidecar, serialization gotchas (initializer defaults, `DirectionalFractions` authored flags) |
| Death fog / corruption / blight spread | [death-fog.md](death-fog.md) ✅ | The `DeathFogSystem` scalar-field spread sim (`Update` = sources→diffuse→sinks→ground corruption), tree/ground/grass corruption; ticked from `UpdateAnimations` on **`GameClock.WorldDt`** so pause AND editors freeze it (the historical spreads-in-editor leak is fixed) |
| Save / load games — `saves/{name}.json` session snapshot | [save-load.md](save-load.md) ✅ | The play-session save feature (distinct from map save): `Data/SaveGameData.cs` DTOs (`SaveGameData`/`SavedPlayer`/`SavedBuff`/`SaveGameInfo`), `Game1.Saves.cs` write/load/apply pipeline (`WriteSaveGame`/`LoadSaveGame`/`ApplySaveToWorld`), `UI/SaveGameWindow.cs`, preview cards; **where a new persisted field goes** + the player `_inventory` (runtime store + why it's not saved yet) |
| Logging & diagnostics | [logging-diagnostics.md](logging-diagnostics.md) ✅ | `Core/DebugLog.cs` (`log/<tag>.log` files, tag census, no in-memory store), `Game/CombatLog.cs` (`Simulation.CombatLog` 200-entry message list + HUD fading lines `HUDRenderer.DrawCombatLog`), frame timings (`Simulation.LastTickMs`/`LastPhaseMs`, `Game1._drawMsAvg` readout, perf-spike logger), startup timings (`Game1.LogTiming` → `log/startup.log`), live world counts (`GameSession.Census()`) |
| User settings — per-machine `user settings/settings.json` | [user-settings.md](user-settings.md) ✅ | The gitignored per-machine settings/config: `GameSettingsData` schema (section POCOs, `[JsonPropertyName]`, `Load`/`SaveIfChanged`) in `Data/Registries/GameSettings.cs`, first-run seeding via `GamePaths.SeededUserFile`, `Game1.Exiting` persistence, the `Display` "remember how the user left X" pattern, plus how weather.json/spellbar.json use the same mechanism. **Where to add a new persisted user setting.** |
| Scenarios — coded headless tests & batch sim harnesses | [scenarios.md](scenarios.md) ✅ | `Necroking/Scenario/` harness (`ScenarioBase` contract + Game1 `StartScenario` run path, flat-grass `GridSize` world, no map load), `ScenarioRegistry` registration, the `balance_matrix` unit-balance tournament (arena duels, swing self-resolution at batched tick speed, win/casualty stats → `log/balance_results.json`, `tools/balance_report/make_report.py` HTML matrix) |

## Subsystems (under `Necroking/`) — most not yet documented

Folder purposes and "where does new code go" (incl. the namespace exceptions) are
authoritative in [docs/code-map.md](../code-map.md); this table is the routing view.

| Folder / area | Tentative responsibility (verify) | Doc |
|---------------|-----------------------------------|-----|
| `Game1.*` (root) | Top-level `Game1` partial class: loop, glue, player-facing entry points | game1-partials.md ✅ |
| `Core/` | Engine-agnostic primitives — `Vec2`, `GameClock`, `GamePaths`, `InputState`, `DebugLog`, JSON/file utils | (not yet documented) |
| `Data/` | Game-data model + JSON registries (spells, units, items, potions, buffs, weapons, armor) under `Data/Registries/` | [data-registries.md](data-registries.md) ✅ |
| `Game/` | Gameplay systems — `Simulation` (the headless, deterministic world model ticked by `Game1` via `GameSession`; the Simulation-vs-Game1 boundary rule is in the class comments on both), spell targeting (`SpellCasting`), spell effects (`SpellEffectSystem`), crafting/table-craft, inventory, horde caps, scripted motion (`Trample`/`Jump`), `DeathFogSystem`, `WeaponBonusEffect`. Spans three namespaces — see [code-map.md](../code-map.md) | [death-fog.md](death-fog.md) ◐ (DeathFogSystem only) |
| `Game/SkillEffects/` | Skill/passive effect application (`SkillEffects.cs`) | (not yet documented) |
| `Render/` | Rendering subsystems — atlases, shadows, bloom, font manager | [render.md](render.md) ◐ (effects only) |
| `UI/` | Overlays & panels — HUD renderer, grimoire, skill book, character stats/sheet, unit info, inventory/crafting/building menus, multiplayer window, widget renderer | [ui.md](ui.md) ✅ |
| `World/` | World/environment systems — env objects, foragables, walls, roads | [world.md](world.md) ✅ |
| `Movement/` | Unit data model + ORCA solver + facing util (pathfinder itself is in `World/Pathfinder.cs`) | [movement.md](movement.md) ✅ |
| `AI/` | Unit AI behaviors & routines (combat, routines like craft-at-table) | [ai.md](ai.md) ✅ |
| `Spatial/` | Spatial partitioning / grid queries | (not yet documented) |
| `Algorithm/` | Standalone algorithms | (not yet documented) |
| `Editor/` | In-game immediate-mode editors (unit / spell / map / UI / item) | [editor.md](editor.md) ◐ (UI/widget editor only) |
| `Dev/` | Dev control server — `DevServer`, `DevCommand` (HTTP → `ExecuteDevCommand`) | (not yet documented) |
| `Scenario/` | Coded headless test scenarios (~125 files, `--scenario <name>`) | [scenarios.md](scenarios.md) ✅ |

## Behavior → area quick index

Use this to pick a starting area. When the routed area isn't documented yet, document it.

- **A spell does the wrong thing / adding a spell** → game1-partials.md (`Game1.Spells.cs`) + `Game/` (SpellCasting, SpellEffectSystem) + `Data/Registries/` (SpellRegistry). See also `docs/spells.md`.
- **Cast begin/end lifecycle, "is the necromancer casting", cast-time/channel state, gate/lock something for the cast duration (e.g. stop moving while casting)** → game1-partials.md (`_pendingCastAnim` field: set in `Game1.Spells.cs` `DispatchSpellCast`, cleared in `Game1.Animation.cs` `UpdateAnimations`/`UpdateChanneledCast`) + [movement.md](movement.md) (player movement gate = `Simulation.UpdateAI` `PlayerControlled` case, `PreferredVel = _necroMoveInput * speed`, with the `CorpseInteractPhase` zero-`PreferredVel` precedent).
- **Crafting / gathering / table craft** → game1-partials.md (`Game1.Crafting.cs`) + `Game/` (table-craft system).
- **Corpses — data model, picking up / carrying a corpse, click/F-key corpse interaction, corpse piles** → [corpses.md](corpses.md) (`Game/Simulation.cs` `Corpse`, `Game/CorpseInteractionManager.cs`, `Game1.cs` input + `_hoveredCorpseIdx`, `Game/ForagableSystem.cs` for the gather pattern).
- **Workers / jobs — assigning workers, job board, grave roster, auto-dispatch** → [jobs-workers.md](jobs-workers.md) (`UI/JobBoardUI.cs`, `UI/GraveRosterUI.cs`, `Game/Jobs/WorkerSystem.cs`, `AI/WorkerHandler.cs`).
- **Animation — unit anim state, who sets/changes it, dodge/block/flinch/death overrides, "unit slides while playing the wrong anim", anim priority/locks, cast-phase timing, attack anims** → [animation.md](animation.md) (`Render/AnimController.cs` `AnimState` + policy tables, `Render/AnimResolver.cs` `SetOverride`/`Resolve`, `Game1.Animation.cs` `UpdateAnimations` + the writer census) + game1-partials.md for the channeled-cast tick.
- **Rendering looks wrong (world / units / corpses / HUD / spellbar)** → game1-partials.md (`Game1.Render.*`) + `Render/`.
- **Draw pipeline / render order / passes / shaders-per-draw / blend-sampler state / where a new render pass or pass-dispatcher goes** → [render.md](render.md) "The draw-dispatch pipeline" (`Necroking/GameRenderer.Draw.cs` `Draw()` = the pass sequence; `Render/EffectBatch.cs` = canonical state + flush-with-shader `BeginEffect`; `Render/Bloom.cs` = RT/compositing; `_g._depthItems` Y-sort in `GameRenderer.Units.cs` = the only draw queue).
- **HUD / spellbar / on-screen overlays** → game1-partials.md (`Game1.Render.HUD.cs`) + `UI/`.
- **Debug F-key toggles (F2/F3/F5/F6/F7/F8 cycle debug overlays/modes), where the player-data top-left status bars are drawn, reusable dropdown/combo widget for a HUD panel** → [render.md](render.md) "Debug F-key toggles & debug overlays" + "The player-data HUD" + "Reusable dropdown / panel widgets" (input block in `Game1.cs` Update; fields `_collisionDebugMode`/`_gameplayDebugMode`/`_waterDebug`/`_windDebug`/`_showPerfReadout` + `DeathFogSystem.ToggleDebug`; bars = `UI/HUDRenderer.cs` `DrawStatusBars` at `BarX=10,y=30`).
- **Dropdowns / combo boxes anywhere — the eager press→drag→release gesture, checkmark-gutter option lists, porting or adding a dropdown** → [render.md](render.md) "Reusable dropdown / panel widgets" (`Lib/EagerDropdown.cs` = the shared state machine; `Editor/EditorBase.cs` `DrawCombo`+`DrawDropdownOverlays` = the editor combo, already eager; `UI/DebugSettingsPanel.cs` + `DebugSettingsPanelLayer` in `UI/Layers/HudLayers.cs` = the raw-HUD reference implementation; `Render/DrawUtils.cs` `DrawCheckmark`).
- **Main menu / pause (ESC) menu / scenario list / load menu — buttons, layout, click handling, adding a menu screen** → one class per screen in `UI/`: `MainMenuScreen.cs` / `PauseMenuScreen.cs` / `ScenarioListScreen.cs` / `LoadMenuScreen.cs` (each = private single-source `BuildLayout` + `Draw` + `Update`/`HandleClick`; shared button/backdrop/panel/scrollbar style + `MenuButtonId`/`MenuButton` in `UI/MenuCommon.cs` static `MenuDraw`). Game1.Update's `_menuState` blocks call `screen.Update()`; draw dispatch = `GameRenderer.Draw.cs` early-outs (main/scenario/load, own SpriteBatch) + `MenuHostLayer` in `UI/Layers/HostLayers.cs` (pause — blocking blanket + ESC walk-back). Scroll offsets `_scenarioScrollPx`/`_loadMenuScrollPx` + `_loadMenuSaves`/`_backMenuState` stay Game1 fields (Game1.Dev.cs pokes them); drag state lives on the screens. Game-over stays `GameRenderer.Hud.cs` `DrawGameOver`; submenu pattern = `WantsClose` window classes `Editor/SettingsWindow.cs`/`UI/MultiplayerWindow.cs`/`UI/SaveGameWindow.cs`.
- **Drawing an icon by path (spell/item icon), a UnitDef portrait, or where a shared image/texture cache lives (usable from EditorBase windows AND HUD code)** → [render.md](render.md) "HUD/UI icons & unit portraits" (`SpellDef.Icon` → `RuntimeWidgetRenderer.DrawIcon` (shared `Game1._widgetRenderer`) → `Render/TextureCache`; `GameRenderer.DrawUnitIdleSprite` = UnitDef→portrait via the `DrawUnitIconCallback` seam; skill "Become X" icons are static `assets/UI/Icons/Skills/{id}.png` PNGs, not portraits; `EditorBase.Scope` shares Game1's `SpriteBatch`).
- **HUD panel open/close toggling, panel keyboard shortcuts (I/C/B/J/K/Tab/U/O), menu-button clicks, panel mutual-exclusion / which side a panel docks to (left/right/center)** → [ui.md](ui.md) "Panel open/close toggling & dock sides" (keyboard handlers in `Game1.cs` Update; `GameRenderer.Hud.cs` `ToggleCoreMenu` + `BuildMenuOpenMask`; per-panel side table; the duplicated building↔crafting mutual-close is the existing exclusivity precedent).
- **Spell list / spells menu panel position, anchoring, or its tab/tile hit-testing (the Grimoire)** → [ui.md](ui.md) (`UI/GrimoireOverlay.cs` → `Layout(screenH)` sets `_x`/`_y`, shared by draw + hit-test).
- **Selected/inspected unit right-side sheet, grimoire, skill book, character sheet panels; which panel shows for a selected entity; corpse-pile contents display; boar belly panel** → [ui.md](ui.md) (`UI/UnitInfoPanel.cs`, decision in `Game1.cs` 'U'/'O'/auto-hover, `HUDRenderer.DrawObjectTooltip` + `WorkerSystem.PiledCorpseLines` for pile contents, `Simulation._boarBellies` for belly data).
- **Unit stats / spell / item editors — where each editor's editable fields are declared, per-field hover tooltips** → [editor.md](editor.md) "Spell editor & unit editor" (spell fields = `[EditorField]` attrs on `SpellDef` in `Data/Registries/SpellRegistry.cs` rendered by `Editor/ReflectionPropertyRenderer.cs` `DrawField`; unit fields = hand-rolled `Draw*Section` methods in `Editor/UnitEditorWindow.cs`; tooltip precedent = `SettingsWindow.RowTip` → `Game1.Tooltips`).
- **Map editor — adding a tab/category, object brush painting, placement spacing, map save (`placedObjects`)** → [editor.md](editor.md) "Map editor" (`Editor/MapEditorWindow.cs`: `MapEditorTab` enum + `TabRow1/2` + `_tabScroll` + Update/Draw switches; `PaintObjectsBatch` + `EnvironmentSystem.CanPlaceObject`; `SaveMap`).
- **Map editor Units tab — the placeable-unit list/picker, placing/deleting units, unit thumbnails in editor UI** → [editor.md](editor.md) "Units tab" bullets (`Editor/MapEditorWindow.cs` `DrawUnitsTab`/`UpdateUnitsTab` + the `_unitListDrawY` cached-layout hit-test; static unit sprite thumbnail = `UnitDef.SpriteData` → Idle frame → `atlas.GetTextureForFrame`, per `UnitEditorWindow.DrawPreviewSprite`). No editor image-grid picker or `EditorBase` tooltip helper exists yet.
- **Editor text field: typed value bleeds into the next selected object, field focus/commit, buffer binding** → [editor.md](editor.md) (`Editor/EditorBase.cs` `DrawTextField` + single `_activeFieldId`/`_inputBuffer` + `HandleTextInput`; no flush on selection change in per-editor `DrawScrollableList` handlers — single-point-of-fix is in `EditorBase`).
- **List scrolling / scrollbars anywhere — draggable proportional scrollbar, map-editor tab scroll state (`_tabScroll`/`_envListScroll`), scroll panels, scenario/main-menu list scroll** → [editor.md](editor.md) "Map-editor scrolling & scrollbars" (single source of truth `UI/VScrollbar.cs` geometry+palette; `Editor/EditorBase.cs` interactive `DrawVScrollbar(id,…)` + `BeginScrollPanel`/`ScrollPanel` + `HandlePanelScroll`/`SetPanelContentHeight`; raw-HUD-menu draggable precedent = scenario list: `Game1.cs` `_scenarioScrollPx` input + `GameRenderer.Hud.cs` `DrawScenarioScrollbar`/`ScenarioMenuView`; `Editor/MapEditorWindow.cs` per-tab census). Save/load menus don't scroll yet — see [save-load.md](save-load.md) "Menu list scrolling".
- **UI/widget editor — data models, copy/paste/duplicate a widget or child, widget/element field list** → [editor.md](editor.md) (`Editor/UIEditorWindow.cs`: `CloneWidget`/`CloneChild` do the copy but omit fields; model classes `UIEditor{NineSlice,Element,Child,Widget}Def` at the top of the file; runtime consumers `UI/RuntimeWidgetRenderer.cs`/`UI/WidgetLayoutUtils.cs`).
- **Save/load a game, what gets written to a save file, adding a field to the save (inventory, mana, skills, horde), the save/load menu or save-preview cards, restoring player state on load** → [save-load.md](save-load.md) (`Data/SaveGameData.cs` = the serializable model; `Game1.Saves.cs` `WriteSaveGame`/`ApplySaveToWorld` = write+restore; player inventory runtime store = `Game1._inventory` : `Necroking.GameSystems.Inventory` in `Game/Inventory.cs`). NOT map save (that's `SaveMap` → `placedObjects`, see editor.md).
- **Adding/reading a persisted per-machine user setting, "remember X across restarts", window/display state, gameplay toggles, editor tunables, weather/spellbar persistence** → [user-settings.md](user-settings.md) (`Data/Registries/GameSettings.cs` `GameSettingsData` + section POCOs; `Core/GamePaths.cs` `SeededUserFile` + `UserSettingsJson`; `Game1.cs` `Exiting` handler; the `Display`/`ApplyWindowMode` "persist a live change" precedent). NOT the id-keyed registries.
- **Game data values (units, items, spells JSON)** → `Data/` + `data/*.json` (use the `edit-game-data` skill to edit those).
- **How `data/*.json` is loaded/saved/serialized — registry save path, JSON options, omit/format fields, missing-field defaults, adding a UnitDef/SpellDef field, editor Save button flow** → [data-registries.md](data-registries.md) (`Data/Registries/RegistryBase.cs` `CreateJsonOptions`/`Save`/`Load`; `Data/GameData.cs` `Save` = save-everything called by editor `SaveAll`; `Data/Registries/UnitRegistry.cs` `UnitDef`).
- **Adding a new per-unit combat stat (a number combat rolls read, like Morale/Attack) / how UnitDef stats become runtime `unit.Stats`** → [data-registries.md](data-registries.md) "Adding a new per-unit combat stat" (`UnitStatsJson` + `UnitStats` in `Data/CombatTypes.cs` + the `UnitRegistry.BuildStats` copy + `Simulation.ApplyDefRuntimeFields`).
- **Melee/ranged attack resolution, "am I in range to hit", melee lands at wrong range, player click→attack order, where `PendingAttack` is stamped/resolved** → [combat.md](combat.md) (`Game/Combat/MeleeRangeUtil.cs` = the range formula; `Game/Simulation.cs` `ResolvePendingAttack`/`ResolveMeleeAttack` + attack-selection loop; `Game1.WorldClicks.cs` `TryAttackClick` = player click order; `Game1.Animation.cs` hit-frame trigger). Range is gated at **stamp** time, not resolve time.
- **Unit AI / routines / combat decisions / adding a per-unit behavior or archetype** → [ai.md](ai.md) (`AI/IArchetypeHandler.cs` registry, `AI/SubroutineSteps.cs` move primitives, `AI/WorkerHandler.cs` FSM template; register in `Game1.cs`).
- **Routine transitions / interrupting a unit's AI from outside / OnRoutineExit cleanup / "unit stuck after external system touched it"** → [ai.md](ai.md) "Shared transition logic" (`AI/AIContext.cs` `TransitionTo`, `AI/AIControl.cs` `Interrupt`/`StartRoutine`, `ai_trace` dev command). Never write `Unit.Routine` raw.
- **Archer/ranged unit AI — kiting, firing arrows, ranged weapon selection** → [ai.md](ai.md) (`AI/RangedUnitHandler.cs`) + [combat.md](combat.md) "Ranged / projectiles" for the arrow-spawn pipeline.
- **Wild predator AI — wolf/boar hit-and-run, bear wait-for-facing-away ambush** → [ai.md](ai.md) (`AI/SoloPredatorHandler.cs`: SoloPredator=15 hit-and-run, AmbushPredator=16 opportunist; replaced the deleted legacy Wolf* AIBehaviors/`UpdateWolfAI`).
- **NPC spell-casting AI / unit mana** → [ai.md](ai.md) (`AI/CasterUnitHandler.cs`, CasterUnit archetype; `MaxMana`/`ManaRegen`/`SpellID` copied by `Simulation.ApplyDefRuntimeFields`).
- **Projectiles — arrows, lobbed/arcing shots, explosives, potion lobs, projectile collision** → [combat.md](combat.md) "Ranged / projectiles" (`Game/Projectile.cs` `ProjectileManager.Spawn` — the ONE spawn entry point; `ProjectileType` RegularHit/Explosive/Potion picks contact behavior, `lob` picks flat vs ballistic arc; resolve in `Game/Simulation.cs` `ResolvePendingAttack` ranged branch). Note: no wall collision, no LoS utility exists.
- **Pounce / leap attacks — intercept prediction, landing point, jump phases/anim timing, pounce misses** → [combat.md](combat.md) "Pounce intercept lead" + "Pounce timeline" (`Game/Simulation.cs` `InitiatePounceWithWeapon`, `Game/Combat/InterceptUtil.cs`, `Game/JumpSystem.cs` `BeginPounce`/`FireLandingCallback`; ticked from `Game1.Animation.cs` `JumpSystem.TickUnit`).
- **Fly height / vertical draw offset (a unit lifted off the ground), a voluntary scripted jump/leap ability, "make a unit hop", `Unit.Z`, `SetFlyHeightTmp` equivalent** → [combat.md](combat.md) "Voluntary (non-pounce) jump" + "Fly height" (`Game/JumpSystem.cs` `BeginJumpAttack` = stationary arc leap, Space-key precedent in `Game1.cs`; `Unit.Z` = the height field, rendered via the `Camera25D.WorldToScreen` height arg + `GameRenderer.Units.cs`; ticked by `JumpSystem.TickUnit` from `Game1.Animation.cs`). Involuntary knockback version = `Game/PhysicsSystem.cs`.
- **Knockback / physics impulse / launched units / "unit goes flying" / spell knockback fields / projectile-hit damage application** → [combat.md](combat.md) (`Game/PhysicsSystem.cs` `ApplyImpulse`/`ApplyRadialImpulse`; the `_projectiles.Hits` consumer loop in `Game/Simulation.cs` `Tick`; `SpellDef.Knockback*` in `Data/Registries/SpellRegistry.cs`; spell-projectile config chokepoint `SpellEffectSystem.SpawnProjectile`).
- **Floating text / damage numbers ("Too Far", "+N" pickups) — spawn site, drift, head-height positioning** → [render.md](render.md) "Floating text / damage numbers" (`DamageNumber` struct in `Game/SpellEffectSystem.cs`, `Game1.cs` `SpawnCastFailText` + update tick, `GameRenderer.World.cs` `DrawDamageNumbers`; note the `Height * YRatio` vs sprite-height mismatch).
- **Does something block at X / can a unit stand here / walkable spot check / teleport-dodge landing / spawn position validity / placement overlap rejection / env collision circle math** → [blocking.md](blocking.md) (`_sim.Query.IsSpotBlocked`/`IsWallBlocked` in `Game/WorldQuery.cs`; `World/TileGrid.cs` `OverlapsImpassable`; `World/EnvironmentSystem.cs` `GetCollisionCircle` + `CanPlaceObject`; the collision-dirty sync flow).
- **Pathfinding / movement — units path wrong, get stuck, crowd avoidance, wall collision, formation slots, acceleration/gait feel** → [movement.md](movement.md) (`World/Pathfinder.cs` `GetDirection` = the flow-field pathfinder + imaginary-chunk stuck escape; `Movement/Orca.cs` = ORCA solver; `Game/Simulation.cs` `UpdateMovement` = per-frame gather/solve/collide; `Game/HordeSystem.cs` = formation slot targets).
- **Death fog / corruption / blight spread — fog diffusion, trees/ground/grass turning corrupted, blight bomb** → [death-fog.md](death-fog.md) (`Game/DeathFogSystem.cs` `Update` = the spread tick; called from `Game1.Animation.cs` `UpdateAnimations`, which runs on `GameClock.WorldDt` so pause AND editors freeze it).
- **Time / pause / dt — "what advances while paused / in an editor", which dt a new update should take, time scale, world time vs visual time, adding a pause source** → [game1-partials.md](game1-partials.md) "GameClock — time & pause authority" (`Necroking/Core/GameClock.cs`; sim gate = `_clock.WorldRunning` in `Game1.cs` Update; world age = `Simulation.GameTime`, visual phase clock = `GameClock.VisualTime`).
- **Environment objects / foragables / mushrooms / walls / world content / runtime world-object spawning / eating foragables / on-death drops** → [world.md](world.md) (`World/EnvironmentSystem.cs` `AddObject`/`FindDef`/`CollectForagable`, `Game/ForagableSystem.cs`, death hook in `Game/Simulation.cs` `RemoveDeadUnits`; and map content lives in `data/maps/`, not code).
- **Per-game state ownership / memory leaks across map reloads / where a new per-game system or resource should live / state bleeding from one map to the next** → [game1-partials.md](game1-partials.md) (`Necroking/Game/GameSession.cs` — the recreated-each-load owner Game1 forwards to; `StartGame` does `_session.Dispose(); _session = new()`). App-vs-game classification + migration checklist in `todos/gamesession-migration.md`; `mem` dev command for spotting reload leaks.
- **Map zones / areas — village, wolf-pack, deer-herd, foraging rects; where areas are defined, edited (map editor Zones tab), saved, applied at load; per-zone config (population, spawn tables); periodic zone respawn** → [zones.md](zones.md) (`Game/ZoneTypes.cs` `MapZone`/`ZoneKind`/`ZoneSpawnEntry`, `Game1.Zones.cs` `ApplyZones` + `UpdateZoneSpawns`, `Data/MapData.cs` `LoadZones`, `Editor/MapEditorWindow.cs` Zones tab + `DrawZoneLeftPanel`/`DrawZoneSpawnPanel` + `SaveZones`). No in-game click path exists.
- **Squads — herds/packs/patrols as one group; creating/finding a squad, membership, centroid/spread, squad culling** → [ai.md](ai.md) `SquadSystem.cs` bullet (`AI/SquadSystem.cs` `CreateSquad`/`TryGet`/`Update`=`AssignUnassigned`+`Recompute`; `AIContext.MySquad`; `Simulation.Squads`).
- **Per-frame mouse/keyboard capture, click pressed-this-frame edges, window-focus (`IsActive`) gating of input, "click while unfocused registers in-game", run-when-unfocused** → [game1-partials.md](game1-partials.md) "Per-frame input capture + window-focus gate" (`Necroking/Core/InputState.cs` `Capture` computes `LeftPressed`/`RightPressed` edges; the `IsActive`/`unfocused`/`cursorOutside` gate + refocus-click protection is at the top of `Game1.Update` in `Necroking/Game1.cs`, ~lines 2627-2684). The gate is **already implemented**.
- **Tooltip drawn behind/under another HUD element (spellbar tooltip vs aggro bar), cursor-tooltip z-order, the topmost Tooltip band** → [ui.md](ui.md) "Cursor-tooltip z-order trap" (`HUDRenderer.Draw` tooltip calls run inside `HudChromeLayer`, bottom of the Hud band; `AggressionBarLayer` draws after; `UIBand.Tooltip` = 900 is seated by `TooltipHostLayer` draining `Game1.Tooltips`).
- **Adding a hover tooltip to any control / editor row / dropdown option (draws topmost, above overlays and scissor clips)** → [ui.md](ui.md) (`UI/TooltipSystem.cs` = the global frame-scoped request queue, `Game1.Tooltips.RequestLines/RequestText/RequestCustom`, drained by `TooltipHostLayer` at `UIBand.Tooltip`; editor-row precedent = `Editor/SettingsWindow.cs` `RowTip`; delayed-hover precedent = `UI/StatTooltips.cs` `HoverDelay`/`ReshowGrace` timer — TooltipSystem itself has no delay).
- **MouseOverUI / UI hit-testing — a world click leaks through a panel, a panel blocks the world, who sets `_input.MouseOverUI`, ContainsMouse rects, popup blanket vs side-panel rects** → [ui.md](ui.md) "MouseOverUI / UI hit-testing" (`Core/InputState.cs` `Capture` reset + `ConsumeMouse`, `UI/PopupManager.cs` `RouteInput` stack scan, the ad-hoc bounds block in `Game1.cs` Update, `Editor/EditorBase.cs` `SetMouseOverUI` one-frame-stale propagation).
- **Editor opens then instantly closes/saves when opened by MOUSE (launcher row / pause menu), entry click replays the previous close click, click-consumption doesn't protect immediate-mode editors** → [editor.md](editor.md) "Editor entry-click ghost-replay (FIXED)" (`[X]` flips `_menuState` mid-Draw → skipped `EndDrawFrame` froze `EditorBase`'s mouse state → next mouse-open replayed the old `[X]` click). Fixed by consolidating mouse history into `Core/InputState.cs` (`DrawPrevMouse`/`SnapshotDrawFrame`, `PressStartPos`, `ConsumeGesture` on any `_menuState` transition + `ToggleEditorWindow`). Not `SuppressClicksUntilRelease`.
- **Combat log messages, error/debug log files (`log/*.log`), capturing errors for an in-game panel, frame/startup timings, unit/object counts for a stats readout** → [logging-diagnostics.md](logging-diagnostics.md) (`Game/CombatLog.cs` `Simulation.CombatLog.Entries`; `Core/DebugLog.cs` = the one file-log chokepoint; `GameSession.Census()`; `Game1.LogTiming`).
- **Top-right HUD button rows (core-menu / editor-launcher), adding a button that toggles a panel** → [ui.md](ui.md) UIRouter section + [logging-diagnostics.md](logging-diagnostics.md) callers: `UI/HUDRenderer.cs` `MenuButtonLabels`+`LayoutButtonRow`+`DrawMenuButtons`/`HitTestMenuButtons`, click side `GameRenderer.Hud.cs` `ToggleCoreMenu`/`BuildMenuOpenMask`, seat `UI/Layers/HudLayers.cs` `CoreMenuButtonsLayer` (HudTop band).
- **Dev/test commands driving the running game** → game1-partials.md (`Game1.Dev.cs` → `ExecuteDevCommand`) + `Dev/`.
- **Headless regression tests / writing or extending a `--scenario`** → [scenarios.md](scenarios.md) (`Scenario/ScenarioBase.cs` contract, `ScenarioRegistry.cs` registration, `Game1.cs` `StartScenario` run path; and `docs/testing-scenarios.md`).
- **Balance testing / unit matchup tournaments / win-rate & casualty statistics / batch simulated fights** → [scenarios.md](scenarios.md) (`Scenario/Scenarios/BalanceMatrixScenario.cs` = the arena-duel harness: spawn-side setup, swing self-resolution at batched speed, faction-count winner detection, `log/balance_results.json`; report = `tools/balance_report/make_report.py`).

## When you extend the map

1. Pick the area, glob its folder, read/LSP the files, write `<area>.md` in the
   [game1-partials.md](game1-partials.md) format.
2. Move its row to "Documented areas", mark the doc ✅ here, and tighten its behavior→area
   entries above with the real file names you found.
3. Commit the new doc (the skill is shared).
